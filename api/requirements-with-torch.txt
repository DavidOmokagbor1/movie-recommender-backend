# ML API Requirements WITH PyTorch
# Use this file if you want to enable NeuralMF and DeepFM models
# Note: PyTorch is large and may require paid tier on Render

# Core dependencies
Flask==2.3.3
Flask-Cors==4.0.0
numpy==1.19.3
scipy==1.5.4
scikit-learn==0.24.2
pandas==1.1.4
tqdm==4.54.1
requests==2.31.0

# PyTorch for neural models (NeuralMF, DeepFM)
# Using CPU-only version for smaller size
# Install from PyTorch index for better compatibility
--index-url https://download.pytorch.org/whl/cpu
torch==1.7.1+cpu
torchvision==0.8.2+cpu
torchaudio==0.7.2

