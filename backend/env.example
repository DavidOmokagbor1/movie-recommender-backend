# Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Flask Secret Key (REQUIRED)
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-generate-with-secrets-token-hex

# MongoDB Atlas Connection String (REQUIRED)
# Format: mongodb+srv://<your-username>:<your-password>@<your-cluster>.mongodb.net/?appName=AppleTVPrototype
# Replace <your-username>, <your-password>, and <your-cluster> with your actual MongoDB Atlas credentials
MONGODB_URI=mongodb+srv://<your-username>:<your-password>@<your-cluster>.mongodb.net/?appName=AppleTVPrototype

# MongoDB Database Name (optional, defaults to 'movierecommender')
MONGODB_DB_NAME=movierecommender

# Flask Environment
FLASK_ENV=production

# Python Version (for deployment)
PYTHON_VERSION=3.11.0

# Movie Poster API Keys (optional)
# Get TMDB API key from: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# Get OMDb API key from: https://www.omdbapi.com/apikey.aspx
OMDB_API_KEY=your_omdb_api_key_here

# Database URL (for SQLite fallback, optional)
DATABASE_URL=sqlite:///app.db

# ML Recommendation API URL (REQUIRED for recommendations to work)
# For local development: http://localhost:8000
# For production: https://your-ml-api-service.onrender.com (or your ML API URL)
ML_API_URL=http://localhost:8000
